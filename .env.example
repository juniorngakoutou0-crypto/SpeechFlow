# SpeechFlow Environment Variables Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# FRONTEND (.env.local) - Next.js Configuration
# ============================================================================

# API URLs
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_WS_URL=ws://localhost:4000

# ============================================================================
# BACKEND (.env) - NestJS Configuration
# ============================================================================

# Database Configuration
DATABASE_URL=postgresql://speechflow:password@postgres:5432/speechflow

# Redis Configuration (Queue & Cache)
REDIS_URL=redis://redis:6379

# MinIO Configuration (S3-Compatible Object Storage)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=admin
S3_SECRET_KEY=yourpassword
S3_BUCKET_UPLOADS=uploads
S3_BUCKET_PDFS=pdfs

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Email Configuration (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com

# AI Configuration (OpenRouter)
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxx

# Worker Configuration
WORKER_CONCURRENCY=2

# ============================================================================
# WORKER (.env) - Python Worker Configuration
# ============================================================================

# Redis URL (for job queue)
REDIS_URL=redis://redis:6379

# Database URL (for PDF status updates)
DATABASE_URL=postgresql://speechflow:password@postgres:5432/speechflow

# AI APIs
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxx
GROQ_API_KEY=gsk_xxxxxxxxxxxxx

# MinIO Configuration (File Storage)
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=admin
S3_SECRET_KEY=yourpassword

# ============================================================================
# ADDITIONAL CONFIGURATION
# ============================================================================

# Production URLs (when deploying to domain)
# Uncomment and set these for production deployment
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_WS_URL=wss://api.yourdomain.com

# Worker Performance Tuning
# WHISPER_MODEL_SIZE=large-v3
# WHISPER_DEVICE=cpu
# WHISPER_COMPUTE_TYPE=int8

# Email Domain (for production)
# EMAIL_DOMAIN=yourdomain.com

# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# For Production Deployment:
# 1. Change all default passwords (especially PostgreSQL, MinIO, Redis)
# 2. Generate a strong 32+ character JWT_SECRET
# 3. Use actual API keys from services (OpenRouter, Groq, Resend)
# 4. Configure domain-specific URLs if needed
# 5. Set appropriate email addresses and domains
#
# Keep this file secure - NEVER commit .env to version control!